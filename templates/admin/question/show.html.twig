{% extends 'admin/base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <div class="container">
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ question.label }}</td>
            </tr>
            <tr>
                <td>
                    <a class="btn btn-info" href="{{ path('admin_question_index') }}">Retour à la liste</a>
                
                    <a class="btn btn-warning ml-5" href="{{ path('admin_question_edit', {'id': question.id}) }}">Editer</a>
                </td>
            </tr>
            <tr>    
                <td>
                    <div>{{ include('admin/question/_delete_form.html.twig') }}</div>
                </td>
            </tr>
        </tbody>
    </table>

</div>

{% endblock %}

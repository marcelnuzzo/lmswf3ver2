{% extends "base.html.twig" %}

{% block title %}Liste des users{% endblock %}

{% block body %}
<div class="container mt-5">
<table class="table">
        <thead>
            <tr>
                <th>Utilisateurs</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        
        {% for user in users %}
            <tr>
                <td>{{user.fullname}}</td>
                <td><a href="{{ path('role_assign', {'id': user.id}) }}" class="btn btn-warning">Affecter un rôle</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
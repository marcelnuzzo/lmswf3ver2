{% extends 'admin/base.html.twig' %}

{% block title %}Quizz{% endblock %}

{% block body %}
    <div class="container">
    <h1>Quizz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                
            </tr>
            <tr>
                <td>{{ quizz.id }}</td>
                <td>{{ quizz.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('admin_quizz_index') }}">Retour à la liste</a>

    <a class="btn btn-warning ml-5" href="{{ path('admin_quizz_edit', {'id': quizz.id}) }}">Editer</a>

    {{ include('admin/quizz/_delete_form.html.twig') }}
    </div>
{% endblock %}

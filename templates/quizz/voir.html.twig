{% extends "base.html.twig" %}

{% block title %}Les quizz{% endblock %}

{% block body %}

<div class="container">
{{ form_start(form) }}
    <h3>Questions {{ question.id }} {{ question.label }}</h3>
    
            
            {{form_widget(form)}}
            
            <button class="btn btn-primary" type="submit">
                Valider
            </button>
{{ form_end(form) }}
</div>

{% endblock %}